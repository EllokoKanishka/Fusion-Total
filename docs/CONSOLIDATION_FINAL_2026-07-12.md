# Consolidación final de Fusion Reader v2

> Generated by `scripts/generate_handoff.py`. Git values describe the clean input commit observed before these generated files are committed; GitHub values are separately labeled.

- Generated UTC: `2026-07-13T04:34:45+00:00`
- Branch (Git): `refactor/v2-total-consolidation`
- Head observed before generation (Git): `1eae746241153918b80c617756fa6911f7159382`
- Base ref / SHA (Git): `origin/main` / `19ccaa9bc36b09c008af8245586c82e262ee081a`
- Diff (Git): 64 commits, 219 files, +21492 / -9506


## Architecture after consolidation

- `FusionReaderV2` is the public composition facade and delegates bounded state to ReaderService, AudioService, PreparationService, AudioExportService, DialogueService, ResearchService, NotesService, SessionPersistenceService and BackgroundLifecycleService.
- `fusion_reader_v2/service.py` preserves the historic import without product logic.
- Web request domains live in route modules and are dispatched by `routing.py`.
- The browser has one entry point and ES modules for API, UI, busy state, audio, audio export, preparation, notes and dialogue state.
- CSP rejects inline script/style execution; HTTP remains loopback-only.

## Reproducibility and quality gates

- Python 3.11 and 3.12 have separate pinned constraints consumed by CI.
- Global and critical coverage gates consume `coverage.json` and fail independently.
- Subprocess execution in active paths uses the owned-process helper.
- ZIP/document limits and atomic output publication are covered by unit and integration tests.

## Evidence boundaries

- Local test observation: `550 tests OK on clean Python 3.11 and 550 tests OK on clean Python 3.12; 17 Node tests; 1 Playwright E2E; 3 stress tests`.
- GitHub observation: `observed_github`.
- Nightly is not claimed green unless present as a successful check in the GitHub JSON above.
