# Licensing Decision Required

This repository does not contain a canonical root license selected for Fusion
Reader v2. The consolidation does not choose or change legal licensing terms.

`package.json` historically contains an `ISC` metadata value, but that field is
not treated as a legal license grant for the repository. A human maintainer
must decide the intended license and add the corresponding canonical file and
metadata in a separate, explicitly reviewed change.
