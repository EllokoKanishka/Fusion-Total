# Compatibility stack for the legacy direct-chat STT laboratory.
# sounddevice also requires the host PortAudio runtime (for example libportaudio2 on Debian/Ubuntu).
setuptools==80.9.0
sounddevice==0.5.6
webrtcvad==2.0.10
faster-whisper==1.2.1
ctranslate2==4.5.0
numpy==2.4.6
requests==2.34.2
